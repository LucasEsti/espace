<form method="post" action="{{ path('espace_activite_delete', {'id': espace_activite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ espace_activite.id) }}">
    <button class="btn">Delete</button>
</form>
