{% extends 'base.html.twig' %}

{% block title %}Espace{% endblock %}

{% block body %}
    <h1>Espace</h1>
    <img src="{{ asset('images/' ~ espace.image) }}">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espace.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ espace.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ espace.description }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ espace.active }}</td>
            </tr>
            
        </tbody>
    </table>
            
    <a href="{{ path('espace_index') }}">back to list</a>

    <a href="{{ path('espace_edit', {'id': espace.id}) }}">edit</a>

    {{ include('espace/_delete_form.html.twig') }}
    {% include 'booking/calendar.html.twig' with [{'form': form}] %}
{% endblock %}
